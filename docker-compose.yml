services:
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    ports:
      - "10000:10000"
    environment:
      PORT: 10000

  frontend:
    build:
      context: ./frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
